tinkoffInvestApi {
  token: ${?TINKOFF_TOKEN}
}
slick {
  db {
    properties {
      serverName: ${?POSTGRES_HOST}
      portNumber: ${?POSTGRES_PORT}
      databaseName: ${?POSTGRES_DBNAME}
      user: ${?POSTGRES_USER}
      password: ${?POSTGRES_PASSWORD}
    }
  }
}
shares {
  pctScale: 3
  priceScale: 2
  uptrendThresholdPct: 1  // 3
  numUptrendShares: 10
  incomeTaxPct: 13
  totalPriceLimit: 1000
}
testFlg: true
